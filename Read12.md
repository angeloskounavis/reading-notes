
## Read Class 12

### What does the canvas allow a developer to acheive?

* According to JavaScript Tutorial "allows you to draw 2D graphics using JavaScript. The canvas element requires at least two attributes: width and height that specify the size of the canvas."(link below)

### What is the importance of the closing canvas tag?

* The closing canvas tag works as a fallback content. It will work only in the case that the browser doesn't support the canvas element.

### Explain what the getContext() method does

* It will start the structure pointed to by ucp to the current user context of the calling thread. 

### What is Chart.js and how it can be brought into your project?

* If you would like to create a bar chat for a dataset you would need to to include the script in your page with a single canvas node to render the chart.

### List 3 different Chart types you can create using Chart.js

* Bar chart.
* Line chart.
* Pie chart.

### What are some advantages to displaying data via a chart over a table?

* A chart can create a clearer picture of a set of data values than a table with rows

### How could Chart.js aid your previously created applications visually?

* If you have a numeric data in a graphical format. It would be easier to add and manipulate large quantities of data and the relationship between different series of data.

## Resources

* https://www.javascripttutorial.net/web-apis/javascript-canvas/